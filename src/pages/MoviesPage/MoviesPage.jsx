const MoviesPage = () => {
  // const handleSubmit = (value) => {
  //   setSearchParams({ query: value });
  // };

  return (
    <div>
      <h2>Movies Page</h2>
    </div>
  );
};

export default MoviesPage;
